import type { ReactElement } from "react";

export default function HomePage(): ReactElement {
  return (
    <main>
      <h1>Elura Backend</h1>
      <p>Base project setup is ready. Feature implementation can start from here.</p>
    </main>
  );
}
